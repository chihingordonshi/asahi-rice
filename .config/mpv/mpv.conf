# Hyprland sends native two-finger pinch deltas to this local IPC socket.
input-ipc-server=/tmp/mpv-asahi-gesture.sock

# Preserve simultaneous horizontal and vertical high-resolution scroll events,
# allowing natural diagonal panning on a touchpad.
input-preprocess-wheel=no
